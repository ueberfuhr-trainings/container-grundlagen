apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    # Postgres should only be run in a single POD
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
